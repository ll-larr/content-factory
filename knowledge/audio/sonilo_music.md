---
id: sonilo_music
type: audio
audio_kind: music
family: sonilo
status: verified          # подтверждено живьём 2026-09-05: цена зависит от ДЛИТЕЛЬНОСТИ (10с → $0.02, 30с → $0.08), каталожные $0.0025 к секунде отношения не имеют
output_format: mp3
cost_tier: low
providers:
  # Замер дельтой баланса 2026-09-05: 10с → $0.0200, 30с → $0.0800. Прямая
  # через эти точки не проходит через ноль, то есть тариф не чисто посекундный
  # (похоже на блоки). Берём ВЕРХНЮЮ наблюдённую ставку $0.00267/с ($0.08/30с):
  # смета должна ошибаться в большую сторону, а не в меньшую.
  wavespeed: { id: "sonilo/text-to-music", pricing: flat, usd_per_sec: 0.00267, fields: { prompt: prompt, duration: duration } }
---

# Sonilo Text-to-Music — фоновая музыка

## Структура запроса

```
prompt   = "<стиль, настроение, инструменты, аранжировка>"
duration = <секунды, максимум 360>
```
