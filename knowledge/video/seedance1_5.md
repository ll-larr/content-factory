---
id: seedance1_5
type: video
family: seedance
status: verified          # WaveSpeed подтверждён живьём 2026-08-01 (bytedance/seedance-v1.5-pro/image-to-video — id БЫЛ ДОГАДКОЙ, подтверждён; 4с 720p start+end → файл 1280x720 h264 4.05с со звуком aac, списано $0.21 по разнице балансов, смета обещала $0.20)
supports_start_end_frame: true
native_audio: true
max_clip_seconds: 12
allowed_durations: [4, 8, 12]
aspect_ratios: ["auto","16:9","9:16","4:3","3:4","1:1","21:9"]
cost_tier: low
providers:
  wavespeed:              # Seedance 1.5 Pro — самая дешёвая со start/end; id и цена ПОДТВЕРЖДЕНЫ живьём 2026-08-01
    supports_start_end: true
    pricing: scaled
    res_mult: {720p: 1.0, 1080p: 2.0}   # из формулы каталога: 720p=1, 1080p=2 (прежние 1.4 были догадкой)
    allowed_durations: [4, 8, 12]
    tiers:
      pro: { id: "bytedance/seedance-v1.5-pro/image-to-video", usd_per_sec: 0.053 }  # формула каталога: duration*0.26/5*(generate_audio?1:0.5) → 4с со звуком = $0.208, но живьём списано $0.21 — округлено ВВЕРХ до 0.053 (4с = $0.212), чтобы смета не занижала фактическое списание
    default_tier: pro
  # Runware/OpenRouter — добавить после подтверждения хостинга/id/цены на спайке
---

# Seedance 1.5 Pro (seedance1_5)

**Важно: сетка длительностей 4/8/12с НЕ совпадает с отрезками 5/10с конвейера** —
перед использованием согласовать с логикой нарезки (спека §4).

Самая дешёвая модель со start/end из проверенных (цена — в providers-блоке, смета `estimate_media_cost`).

## Параметры (разведка 2026-06-12 — сверить с providers-блоком и docs провайдера на спайке)

- входные кадры: start + end (имена полей провайдера — в providers-блоке/адаптере)
- `duration` — сетка `4` / `8` / `12` (не 5/10! — валидатор отбивает несовпадение)
- `aspect_ratio` — `auto`, `16:9`, `9:16`, `4:3`, `3:4`, `1:1`, `21:9`
- **Start/end кадры:** поддержка подтверждена ✓
- Цена — в providers-блоке (смета: `estimate_media_cost`, в $).

## Проверено живьём 2026-08-01 (WaveSpeed)

4с 720p со start+end → 1280x720 h264, 4.05с, с нативной aac-дорожкой; списано $0.21
против сметы $0.20. Путь `bytedance/seedance-v1.5-pro/image-to-video` был догадкой —
подтверждён. НЕ проверены: 1080p, длительности 8/12с, Runware/OpenRouter.

## Наблюдения раннего теста (reference, до мультипровайдерного спайка)

- **Start/end интерполяция работает**: первый кадр видео = start-кадр, последний =
  end-кадр, движение плавное, персонаж стабилен (4с, 1280×720, 24fps).
- Файл может прийти с аудиодорожкой — для конвейера звук игнорируем: аудио идёт
  отдельной дорожкой (TTS/музыка/SFX — задача на ElevenLabs).

## Сильные стороны
(карточка-скелет: наполняется при первом использовании модели)

## Структура промпта движения

Кадры задают композицию — промпт описывает только движение. На спайке сработал
короткий промпт «the cat slowly reaches toward the control panel, subtle camera
push-in»: движение и камера переданы корректно.

## Типовые ошибки

**Объекты «из воздуха» при несостыкованных кадрах** (спайк 2026-06-12):
модель честно интерполирует между start и end — если в end-кадре есть объект,
которого нет в start-кадре (кнопка, панель), он «вырастает в воздухе» посреди
отрезка. Для отрезка ВНУТРИ одной сцены это НЕ дефект модели, а ошибка подготовки
пары кадров: объект взаимодействия и ключевые объекты сцены должны присутствовать
в ОБОИХ кадрах. Если же несовпадение кадров намеренное (сюжетное изменение
окружения или transition-отрезок между сценами) — само изменение/переход обязан
быть описан в промпте движения, иначе модель придумает его «как получится».
Классификация типов отрезков и правила пар — в карточке nano_banana_flash
(best practices для start/end пар). Пары start/end сверять с типом отрезка
на ревью кадров ДО запуска видео.
