---
id: minimax_hailuo
type: video
family: hailuo
status: skeleton
supports_start_end_frame: false
native_audio: false
max_clip_seconds: 10
allowed_durations: [6, 10]
aspect_ratios: []
cost_tier: low
---

# Minimax Hailuo (minimax_hailuo)

End-кадр НЕ принимает (проверено 2026-06-12: CLI ответил «Model accepts only --image») — для схемы склейки отрезков непригодна.

**Важно: сетка длительностей 6/10с НЕ совпадает с отрезками 5с конвейера.**

## Проверено разведкой CLI (2026-06-12, без генерации)

**Параметры:**
- `input_images` — массив входных изображений (только одно изображение, не start+end)
- `duration` — enum `"6"` / `"10"` (не 5!)
- `model` — `minimax` / `minimax-fast` / `minimax-2.3` / `minimax-2.3-fast`
- `resolution` — `512` / `768` / `1080`

**Смета:** 6 кредитов.

**Ожидает спайка (Task 2):** визуальное качество, применимость для планов без end-кадра.

## Сильные стороны
(карточка-скелет: наполняется при первом использовании модели)

## Структура промпта
(карточка-скелет: наполняется при первом использовании модели)

## Типовые ошибки
(карточка-скелет: наполняется при первом использовании модели)
